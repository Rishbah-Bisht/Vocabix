<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Idioms Dashboard</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --daywise-color: #ef4444;     /* Red */
            --allidioms-color: #3b82f6;   /* Blue */
            --addidiom-color: #10b981;    /* Green */
            --mcq-color: #f59e0b;         /* Amber */

            --background-color: #f8fafc;
            --card-color: #ffffff;
            --text-color: #1e293b;
            --text-light-color: #64748b;
        }

        body {
            background-color: var(--background-color);
            font-family: "Poppins", sans-serif;
        }

        .dashboard {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
        }

        header {
            text-align: center;
            margin-bottom: 3rem;
        }

        h1 {
            font-size: 2.5rem;
            color: #2563eb;
            margin-bottom: 0.5rem;
        }

        .options-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }

        a.option-card {
            text-decoration: none;
            color: inherit;
        }

        .option-card {
            background: var(--card-color);
            border-radius: 12px;
            padding: 2rem;
            margin: 0 1rem;
            display: flex;
            flex-direction: column;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
            text-align: center;
            border: 1px solid #e2e8f0;
        }

        .option-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }

        /* ====== Unique Theme Cards ====== */

        .daywise-card {
            border: 1.5px solid var(--daywise-color);
            border-top: 4px solid var(--daywise-color);
        }
        .daywise-card:hover { box-shadow: 0 10px 15px -3px rgba(239, 68, 68, 0.2); }
        .daywise-card .icon { color: var(--daywise-color); }
        .daywise-card .btn { background-color: var(--daywise-color); }
        .daywise-card .btn:hover { background-color: #dc2626; }

        .allidioms-card {
            border: 1.5px solid var(--allidioms-color);
            border-top: 4px solid var(--allidioms-color);
        }
        .allidioms-card:hover { box-shadow: 0 10px 15px -3px rgba(59, 130, 246, 0.2); }
        .allidioms-card .icon { color: var(--allidioms-color); }
        .allidioms-card .btn { background-color: var(--allidioms-color); }
        .allidioms-card .btn:hover { background-color: #2563eb; }

        .addidiom-card {
            border: 1.5px solid var(--addidiom-color);
            border-top: 4px solid var(--addidiom-color);
        }
        .addidiom-card:hover { box-shadow: 0 10px 15px -3px rgba(16, 185, 129, 0.2); }
        .addidiom-card .icon { color: var(--addidiom-color); }
        .addidiom-card .btn { background-color: var(--addidiom-color); }
        .addidiom-card .btn:hover { background-color: #059669; }

        .mcq-card {
            border: 1.5px solid var(--mcq-color);
            border-top: 4px solid var(--mcq-color);
        }
        .mcq-card:hover { box-shadow: 0 10px 15px -3px rgba(245, 158, 11, 0.2); }
        .mcq-card .icon { color: var(--mcq-color); }
        .mcq-card .btn { background-color: var(--mcq-color); }
        .mcq-card .btn:hover { background-color: #d97706; }

        /* Shared Elements */
        .icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        .option-title {
            font-size: 1.5rem;
            margin-bottom: 0.75rem;
        }

        .option-desc {
            color: var(--text-light-color);
            line-height: 1.6;
            flex-grow: 1;
            margin-bottom: 1.5rem;
        }

        .btn {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            color: white;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
            font-size: 1rem;
            margin-top: auto;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2rem;
                color: var(--text-color);
            }
        }
    </style>
</head>

<body>

    <%- include('../partial/nav_bar',{ currentPage: 'idioms' }) %>

    <div class="dashboard">
        <header>
            <h1>Idioms & Phrases</h1>
        </header>

        <div class="options-grid">

            <a href="/idioms-on-date" class="option-card daywise-card">
                <div class="icon"><i class="fas fa-calendar-day"></i></div>
                <h2 class="option-title">Day Wise Idioms</h2>
             
                <span class="btn"><i class="fas fa-list-ol"></i> Take Quiz</span>
            </a>

            <a href="/all_idioms" class="option-card allidioms-card">
                <div class="icon"><i class="fas fa-book-open"></i></div>
                <h2 class="option-title">A–Z All Idioms</h2>
            
                <span class="btn"><i class="fas fa-book"></i> Explore All</span>
            </a>

            <a href="/add-idiom" class="option-card addidiom-card">
                <div class="icon"><i class="fas fa-plus-circle"></i></div>
                <h2 class="option-title">Add New Idiom</h2>
              
                <span class="btn"><i class="fas fa-plus"></i> Add Idiom</span>
            </a>

            <a href="/mcq-idioms" class="option-card mcq-card">
                <div class="icon"><i class="fas fa-question-circle"></i></div>
                <h2 class="option-title">MCQ Idioms Quiz</h2>
          
                <span class="btn"><i class="fas fa-play-circle"></i> Start Quiz</span>
            </a>

        </div>
    </div>

</body>
</html>
